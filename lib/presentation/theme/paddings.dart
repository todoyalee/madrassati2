import 'package:flutter/material.dart';

import '../presentation.dart';

class Paddings {
  Paddings._();

  /// all 4
  static const allXxxxs = EdgeInsets.all(Dimensions.xxxxs);

  /// all 8
  static const allXxxs = EdgeInsets.all(Dimensions.xxxs);

  /// all 12
  static const allXxs = EdgeInsets.all(Dimensions.xxs);

  /// all 16
  static const allXs = EdgeInsets.all(Dimensions.xs);

  /// all 20
  static const allSm = EdgeInsets.all(Dimensions.sm);

  /// all 24
  static const allMd = EdgeInsets.all(Dimensions.md);

  /// all 32
  static const allLg = EdgeInsets.all(Dimensions.lg);

  /// all 40
  static const allXl = EdgeInsets.all(Dimensions.xl);

  /// all 48
  static const allXxl = EdgeInsets.all(Dimensions.xxl);

  /// all 56
  static const allXxxl = EdgeInsets.all(Dimensions.xxxl);

  /// all 64
  static const allXxxxl = EdgeInsets.all(Dimensions.xxxxl);

  /// vertical 4
  static const verticalXxxxs = EdgeInsets.symmetric(vertical: Dimensions.xxxxs);

  /// vertical 8
  static const verticalXxxs = EdgeInsets.symmetric(vertical: Dimensions.xxxs);

  /// vertical 12
  static const verticalXxs = EdgeInsets.symmetric(vertical: Dimensions.xxs);

  /// vertical 16
  static const verticalXs = EdgeInsets.symmetric(vertical: Dimensions.xs);

  /// vertical 20
  static const verticalSm = EdgeInsets.symmetric(vertical: Dimensions.sm);

  /// vertical 24
  static const verticalMd = EdgeInsets.symmetric(vertical: Dimensions.md);

  /// vertical 32
  static const verticalLg = EdgeInsets.symmetric(vertical: Dimensions.lg);

  /// vertical 40
  static const verticalXl = EdgeInsets.symmetric(vertical: Dimensions.xl);

  /// vertical 48
  static const verticalXxl = EdgeInsets.symmetric(vertical: Dimensions.xxl);

  /// vertical 56
  static const verticalXxxl = EdgeInsets.symmetric(vertical: Dimensions.xxxl);

  /// vertical 64
  static const verticalXxxxl = EdgeInsets.symmetric(vertical: Dimensions.xxxxl);

  /// horizontal 4
  static const horizontalXxxxs = EdgeInsets.symmetric(horizontal: Dimensions.xxxxs);

  /// horizontal 8
  static const horizontalXxxs = EdgeInsets.symmetric(horizontal: Dimensions.xxxs);

  /// horizontal 12
  static const horizontalXxs = EdgeInsets.symmetric(horizontal: Dimensions.xxs);

  /// horizontal 16
  static const horizontalXs = EdgeInsets.symmetric(horizontal: Dimensions.xs);

  /// horizontal 20
  static const horizontalSm = EdgeInsets.symmetric(horizontal: Dimensions.sm);

  /// horizontal 24
  static const horizontalMd = EdgeInsets.symmetric(horizontal: Dimensions.md);

  /// horizontal 32
  static const horizontalLg = EdgeInsets.symmetric(horizontal: Dimensions.lg);

  /// horizontal 40
  static const horizontalXl = EdgeInsets.symmetric(horizontal: Dimensions.xl);

  /// horizontal 48
  static const horizontalXxl = EdgeInsets.symmetric(horizontal: Dimensions.xxl);

  /// horizontal 56
  static const horizontalXxxl = EdgeInsets.symmetric(horizontal: Dimensions.xxxl);

  /// horizontal 64
  static const horizontalXxxxl = EdgeInsets.symmetric(horizontal: Dimensions.xxxxl);
}
